# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets
.env
.env.local

# d2p artifacts written into target projects (also under this repo when tested)
.d2p/

# OS / editor
.DS_Store
.idea/
.vscode/

# Editor / agent state
.claude/

# Big local artifacts (not for upstream)
*.zip
稳定版*.zip

# Local-only working docs (session digests, plan scratch, etc.).
# Calibration results are an exception — committed under docs/calibration/.
docs/*
!docs/calibration/

# Local scratch dirs (not for upstream)
just_test/
paper/

# Calibration harness reports (gitignored; commit manually to docs/calibration/)
tests/calibration/reports/
